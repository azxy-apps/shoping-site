/*
import {mongoclient} from "mongodb";
import express from "express";

const app = express();

app.get('/db',async(req,res) =>{
    client = await MongoClient.connect(url);
    database = client.db("shoping-site");
    console.log("connected to mongo");
});

*/
